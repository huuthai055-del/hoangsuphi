import { FoundationPage } from "@/components/layout/foundation-page";

export default function AccountPage() {
  return <FoundationPage title="Tài khoản" description="Nền tảng phiên đăng nhập đã sẵn sàng; hồ sơ đầy đủ phụ thuộc contract backend ở phase tiếp theo." />;
}
