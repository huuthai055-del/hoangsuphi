# 🏆 DATABASE MIGRATION & RUNBOOK CLOSEOUT REPORT: PHASE 4.3 — MEDIA UPLOAD
## 📋 STEP 4.3.1 — Migration Implementation & Catalog Validation Closeout

> **Mã báo cáo:** `CR-4.3.1` | **Trạng thái:** ✅ Đã Hoàn Thành (Completed)
> **Tài liệu chiến lược:** [04.03.01-media-schema-migration-strategy.md](file:///c:/Users/tony/Desktop/youtube/hoangsuphi/backend/docs/04.03.01-media-schema-migration-strategy.md)
> **Drizzle Schema:** [media.ts (Schema)](file:///c:/Users/tony/Desktop/youtube/hoangsuphi/backend/src/lib/database/schema/media.ts)
> **SQL Migration File:** [0016_high_patriot.sql](file:///c:/Users/tony/Desktop/youtube/hoangsuphi/backend/src/lib/database/migrations/0016_high_patriot.sql)
> **Production Runbook:** [04.03.01-media-production-index-runbook.sql](file:///c:/Users/tony/Desktop/youtube/hoangsuphi/backend/docs/04.03.01-media-production-index-runbook.sql)
> **CSDL Kiểm Thử:** PostgreSQL 17.5 cục bộ

---

## 1. MỤC TIÊU & PHẠM VI KHẢO SÁT
Báo cáo này tổng hợp kết quả thực tế của việc chạy transactional migration, thực thi production index runbook, và thực hiện catalog check kiểm chứng trên PostgreSQL 17.5 cục bộ nhằm đảm bảo an toàn dữ liệu, chống race condition, và sẵng sàng chuyển sang Step 4.3.2 (Repository Hardening).

---

## 2. KẾT QUẢ KIỂM THỬ THỰC TẾ

### 🧪 Test 1: Kiểm thử Transactional Migration (Columns & Constraints)
- **Hành động**: Chạy lệnh `bun run db:migrate` để áp dụng file SQL migration `0016_high_patriot.sql`.
- **Kết quả**:
  - Cột `storage_provider` được thêm thành công với kiểu `varchar(50)`, default `'LOCAL'`, và check constraint `media_storage_provider_check` giới hạn giá trị trong `('LOCAL', 'CLOUDINARY')`.
  - Cột `alt_text` (varchar(255)) và `caption` (varchar(500)) được thêm thành công ở trạng thái `nullable`.
  - Check constraint `media_owner_pair_check` được áp dụng thành công.
- **Bằng chứng schema thực tế**:
```
Columns metadata in DB:
┌───┬──────────────────┬───────────────────┬─────────────┬────────────────────────────┐
│   │ column_name      │ data_type         │ is_nullable │ column_default             │
├───┼──────────────────┼───────────────────┼─────────────┼────────────────────────────┤
│ 0 │ storage_provider │ character varying │ NO          │ 'LOCAL'::character varying │
│ 1 │ alt_text         │ character varying │ YES         │ null                       │
│ 2 │ caption          │ character varying │ YES         │ null                       │
└───┴──────────────────┴───────────────────┴─────────────┴────────────────────────────┘

Check constraints in DB:
┌───┬──────────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│   │ conname                      │ definition                                                                                                              │
├───┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 0 │ media_owner_pair_check       │ CHECK ((((owner_type IS NULL) AND (owner_id IS NULL)) OR ((owner_type IS NOT NULL) AND (owner_id IS NOT NULL))))        │
│ 1 │ media_storage_provider_check │ CHECK (((storage_provider)::text = ANY ((ARRAY['LOCAL'::character varying, 'CLOUDINARY'::character varying])::text[]))) │
└───┴──────────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
- **Trạng thái**: 🟢 **PASSED**

---

### 🧪 Test 2: Kiểm thử Preflight Hard Gates & Legacy Compatibility

#### Test 2.1: Duplicate Variants Gate
- **Hành động**: Chèn 2 variant có cùng type `thumbnail` cho một media. Chạy preflight SQL.
- **Kết quả**: Preflight chặn đứng migration thành công và trả về ngoại lệ:
  `Preflight check failed: Duplicate variants detected in media_variants table.`
- **Trạng thái**: 🟢 **PASSED**

#### Test 2.2: Duplicate Metadata Gate
- **Hành động**: Chèn 2 metadata records cho một media. Chạy preflight SQL.
- **Kết quả**: Preflight chặn đứng migration thành công và trả về ngoại lệ:
  `Preflight check failed: Duplicate metadata detected in media_metadata table.`
- **Trạng thái**: 🟢 **PASSED**

#### Test 2.3: Duplicate Active Unbound Gate
- **Hành động**: Chèn 2 media unbound có cùng `uploaded_by` và cùng `hash` ở trạng thái active. Chạy preflight SQL.
- **Kết quả**: Preflight chặn đứng migration thành công và trả về ngoại lệ:
  `Preflight check failed: Duplicate active unbound media (uploaded_by, hash) detected in media table.`
- **Trạng thái**: 🟢 **PASSED**

#### Test 2.4: Active Unbound with NULL Uploaded_by Gate
- **Hành động**: Chèn 1 media unbound active nhưng uploader `uploaded_by IS NULL`. Chạy preflight SQL.
- **Kết quả**: Preflight chặn đứng migration thành công và trả về ngoại lệ:
  `Preflight check failed: Active unbound media rows with NULL uploaded_by detected.`
- **Trạng thái**: 🟢 **PASSED**

#### Test 2.5: Owner Pair Mismatch Gate
- **Hành động**: Chèn 1 media có `owner_type = 'PLACE'` nhưng `owner_id = NULL`. Chạy preflight SQL.
- **Kết quả**: Preflight chặn đứng migration thành công và trả về ngoại lệ:
  `Preflight check failed: Invalid owner pairs detected.`
- **Trạng thái**: 🟢 **PASSED**

#### Test 2.6: Legacy Compatibility Test
- **Hành động**: Chèn 1 dòng media legacy hợp lệ (Phase 3.7). Chạy lệnh DDL thêm cột.
- **Kết quả**:
  - Dữ liệu legacy được bảo toàn nguyên vẹn.
  - Cột `storage_provider` tự động nhận giá trị mặc định là `'LOCAL'`.
  - Cột `alt_text` và `caption` nhận giá trị mặc định `NULL`.
- **Bằng chứng dữ liệu thực tế sau migration**:
```json
{
  "id": "018f6c38-8c10-7000-8000-000000000006",
  "file_name": "legacy.jpg",
  "storage_provider": "LOCAL",
  "alt_text": null,
  "caption": null
}
```
- **Trạng thái**: 🟢 **PASSED**

---

### 🧪 Test 3: Thực thi Production Index Runbook & Catalog Validation
- **Hành động**: Chạy các câu lệnh SQL tạo index concurrently từ tệp runbook [04.03.01-media-production-index-runbook.sql](file:///c:/Users/tony/Desktop/youtube/hoangsuphi/backend/docs/04.03.01-media-production-index-runbook.sql) ngoài transaction block.
- **Kết quả**:
  - Khởi tạo 4 chỉ mục concurrently thành công.
  - Chạy truy vấn catalog check đối chiếu chi tiết:
- **Bảng Catalog đối chiếu thực tế**:

┌───┬─────────────────────────────────────────────────┬────────────┬────────────┬───────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│   │ index_name                                      │ indisvalid │ indisready │ is_unique │ index_definition                                                                                                                                                                                                                                                                                                                │
├───┼─────────────────────────────────────────────────┼────────────┼────────────┼───────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 0 │ media_scoped_hash_idx                           │ true       │ true       │ false     │ CREATE INDEX media_scoped_hash_idx ON public.media USING btree (uploaded_by, owner_type, owner_id, hash)                                                                                                                                                                                                                        │
│ 1 │ media_unbound_active_hash_unique_idx            │ true       │ true       │ true      │ CREATE UNIQUE INDEX media_unbound_active_hash_unique_idx ON public.media USING btree (uploaded_by, hash) WHERE ((owner_type IS NULL) AND (owner_id IS NULL) AND (deleted_at IS NULL) AND ((status)::text = ANY ((ARRAY['UPLOADING'::character varying, 'PROCESSING'::character varying, 'READY'::character varying])::text[]))) │
│ 2 │ media_variants_media_id_variant_type_unique_idx │ true       │ true       │ true      │ CREATE UNIQUE INDEX media_variants_media_id_variant_type_unique_idx ON public.media_variants USING btree (media_id, variant_type)                                                                                                                                                                                               │
│ 3 │ media_metadata_media_id_unique_idx              │ true       │ true       │ true      │ CREATE UNIQUE INDEX media_metadata_media_id_unique_idx ON public.media_metadata USING btree (media_id)                                                                                                                                                                                                                          │
└───┴─────────────────────────────────────────────────┴────────────┴────────────┴───────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

- **Nhận xét**:
  - Cả 4 index đều có **`indisvalid = true`** và **`indisready = true`**.
  - Cấu trúc cột, uniqueness, và biểu thức partial predicate của `media_unbound_active_hash_unique_idx` khớp tuyệt đối 100% với manifest kỳ vọng.
- **Trạng thái**: 🟢 **PASSED**

---

## 3. THỐNG KÊ OPTIMIZER & KHÓA CHẶT
- Đã chạy lệnh `ANALYZE` cập nhật số liệu thống kê cho 3 bảng: `media`, `media_variants`, và `media_metadata`.
- Workspace đã được chạy lệnh `git diff --check` và pass hoàn toàn.
- Linter `bun run lint` (biome lint) pass 100%.

---

## 4. KẾT LUẬN & ĐIỀU KIỆN CHUYỂN STEP 4.3.2
- Toàn bộ các thay đổi database additive, constraints và indexes đều đã chạy thử nghiệm, kiểm chứng catalog hợp lệ và tests đạt.
- Migration transactional và Index runbook đã sẵn sàng để deploy lên staging/production.
- Đủ điều kiện an toàn chuyển sang **Step 4.3.2 — Media Repository Hardening**.

---
*Báo cáo được thiết kế bởi AI Agent Antigravity (Google DeepMind)*
*Cập nhật lần cuối: 2026-07-16*
